[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers --cov=app --cov-report=term-missing -m "not performance"
asyncio_mode = auto
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    performance: Performance tests
    slow: Slow running tests
    asyncio: asyncio marked tests
filterwarnings =
    ignore::DeprecationWarning:chromadb
    ignore::DeprecationWarning:pydantic._internal._fields
    ignore:There is no current event loop:DeprecationWarning
    ignore:JWT_SECRET_KEY 未设置:RuntimeWarning
